<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Details</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-4">
        <h1>Book Details</h1>

        <div class="card">
            <div class="card-body">
                <h5 class="card-title">{{ livre.title }}</h5>
                <p class="card-text"><strong>Number of Pages:</strong> {{ livre.nbrPage }}</p>
                <p class="card-text"><strong>Published:</strong> {{ livre.published ? 'Yes' : 'No' }}</p>
                <p class="card-text"><strong>Category:</strong> {{ livre.category }}</p>
                <p class="card-text"><strong>Author:</strong> {{ livre.author ? livre.author.name : 'No author' }}</p>
                <a href="{{ path('app_list_book') }}" class="btn btn-primary">Back to List</a>
            </div>
        </div>
    </div>
</body>
</html>
